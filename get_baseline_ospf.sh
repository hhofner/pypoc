#!/bin/bash
# Run a single simulation for every configuration in the config directory
for config_file in /config/*.toml; do
done

# Run increasing source node scripts for every configuration in the config directory

# Run increasing 
